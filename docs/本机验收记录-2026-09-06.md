# 本机验收记录（2026-09-06）

## 环境与边界

- Windows 本机、PowerShell 7，隔离数据目录 `.local-acceptance/data`，端口 8010。
- 正式 `backend/data` 验收前后文件清单、大小和修改时间指纹一致：`FF78D5D718169B11CDF3CA1CF1D231D2FFF9E5E134E8641F89607F6B15490F6A`。
- Codex 内置浏览器控制插件初始化返回工具不可用，因此未宣称使用内置浏览器。实际使用项目已安装的 Playwright Chromium，连接真实本机页面和真实 FastAPI 后端；除既有离线 e2e 外，人工流程未拦截或模拟 API。
- 真实兼容与问答调用了 `.env` 中配置的外部模型，产生少量 API 调用。服务器专属 1C1G、systemd、Nginx、HTTPS 项未执行。

## 自动与真实链路

- 后端：79 passed，1 条 Starlette/AnyIO 弃用警告。
- 前端：验收发现并修复偏好快速连续修改的持久化竞态，修复后 12 passed；生产构建成功。
- Chromium 离线流程：桌面/手机 4 passed。
- 真实兼容：Gemini 普通生成、函数调用、SSE、1024 维 embedding、中文 FTS/向量召回、引用 ID 全部 PASS。
- 真实隔离端到端：管理登录、资料上传发布、用户登录、真实问答、引用、原文一致性及状态接口全部 PASS。

## 浏览器通过项

- 独立管理员登录，设置及更换访问码；错误码拒绝、旧码失效、已登录用户继续有效。
- 上传 5 份真实测试资料，草稿预览、备注和适用条件修正、发布、重复包拒绝。
- 真实问答返回 3 至 4 个引用入口；刷新后历史与引用仍在，鉴权下载 DOCX 成功。
- 手动停用后从现行目录隐藏，历史引用仍可打开；v2 备份恢复后 5 份资料重新现行。
- 路径穿越包与原文哈希篡改包均被管理界面拒绝。
- 上传验收新版、选择替代目标、旧版替代停用、版本链两版、阻止直接启用、解除关系后启用。
- 学院、入学年份和指定文件设置刷新后保留；拒绝文件扩展不越界，确认扩展只影响本次。
- 无依据问题没有虚构明确处罚；取消后释放忙碌状态并可再次提问；关闭页面后未完成回答恢复为“已中断”。
- 本地数据导出 JSON 并重新导入；匿名目录和原文件 401，用户令牌访问管理接口 401，错误登录触发 429。

## 安全检查

- `.local-acceptance/兼容性验证.md` 全部 7 项 PASS，不含两项 API 密钥、脚本问题正文或上游错误正文。
- 浏览器下载的 v2 备份含 14 个条目，不含 `.env` 中 Gemini 和 SiliconFlow 密钥。
- 验收截图和导出文件位于忽略目录 `.local-acceptance/browser-evidence/`，不提交 Git。

## 未执行或仅部分验证

- 管理员改密码的最终提交、删除单条对话、清空记录：属于不可逆 UI 操作，留给管理员亲自确认。
- 从原始 PDF/DOCX 重新执行完整提取与打包；本轮只校验并上传既有真实测试包。
- 多领域组合、政策冲突、往年自然语言检索、主题切换、适用条件缺失追问、PDF 页码显示：尚未逐条人工提问。
- 符号链接/高压缩比、向量计数篡改、提示注入、真实断网、切后台、磁盘不足、导入写入故障、恢复期间并发与恢复回滚故障：已有相关自动化或部分证据，但未完成对应人工场景。
- 11 个排队和第 12 个拒绝、100+ 资料、大包、1C1G 内存/延迟/API 用量、systemd OOM、Nginx/HTTPS 与生产日志留存：服务器阶段执行。

结论：本机核心链路可用，验收过程中发现的偏好持久化竞态已修复并回归。当前仍不能满足完整上线门槛，应继续完成清单中保留未勾选的本机人工项和全部服务器专属项。
