# 班级政策问答 Agent 实施计划

## 一、目标与约束
为约 30～80 位同学提供手机、电脑均可使用的政策问答网站，支持领域筛选、跨领域查询和原文溯源；管理员负责资料发布，学生不上传文件。

- 工作目录：`E:\VSCODE-project\campus-policy-agent`，已有本地 Git 仓库。
- 部署环境：Ubuntu 24.04.4，1 核、1GB 内存、10GB 项目磁盘，使用已有域名及 HTTPS。
- 首批资料约 100～500 份，主要为文字版 PDF、Word；原文件和检索数据保存在服务器。
- 聊天使用 Gemini 原生 `/v1beta` 协议反代（地址 `https://sub.xiazir.asia/v1beta`，模型 `gemini-3.8-flash-high`）；embedding 使用硅基流动 `Qwen/Qwen3-Embedding-8B`。地址、模型名及密钥统一保存在根目录 `deploy-config.md`，该文件不入 git、不写入资料包。
- 不增加付费云存储或检索服务；不部署本地模型、OCR、浏览器自动化或独立向量数据库。

## 二、用户端与问答行为
- 使用班级共享访问码进入；管理员经独立的隐藏登录页登录，可在该页重置密码，初始密码为 admin。访问码由管理员后台设置，更换后旧码立即失效；登录状态保存在浏览器本地，换码不影响已登录用户。服务端控制访问权限及请求频率。小范围内部使用，以此安全强度为限。
- 聊天记录、学院、入学年份等个人设置保存在浏览器 IndexedDB；刷新和再次打开可恢复，提供删除、清空、导入及导出，不做跨设备同步。
- 服务端不持久化问答正文、工具结果或反馈；仅在请求处理期间保留必要上下文。日志禁止记录正文和密钥，云端模型仍会接收必要内容。
- 默认“自动选择领域”；用户指定领域时优先查询该范围，必要时自动扩展并说明原因。明确指定文件时严格限定，须由用户点击确认才能扩展。
- 领域支持文档级、章节级多标签；领域选择不改变访问权限，所有持码用户可查看、下载已发布资料。
- 回答区分结论、适用条件、原文依据；点击引用打开标准化原文行号和上下文，并提供对应版本的原文件及 PDF 页码。
- 默认查询当前有效资料，明确询问往年时查询因替代而停用的历史版本；手动停用的资料任何检索不可达。适用范围缺失时追问；政策冲突并列原文，不自行裁决；未找到依据不得解释为“没有处罚”。
- 历史回答保留当时引用，重新提问则查询当前资料；新问题改变主题时重新判断领域，不盲目沿用前一轮范围。

## 三、预处理 Skill 与管理端
- 交付兼容 Codex、Claude Code 的本地预处理 Skill、确定性脚本及资料包格式说明。解析、分块与向量生成全部在本地完成，仅向服务器上传成品资料包（含原文件，用于原文溯源与下载）；服务器不解析文档、不运行 embedding。
- 脚本负责文字提取、文件哈希、稳定行号、分块和格式校验；无文字层的扫描件不做 OCR，在校验阶段直接标记退回，由管理员决定取舍；Agent 辅助提取领域、发布部门、适用范围、生效时间及版本关系，遇到疑点逐项询问管理员，不猜测或改写原文。
- 资料包包含原文件、标准化原文、章节与来源位置、分块、元数据及预生成向量；PDF 保存实际页序号，DOCX 使用章节和行号，不伪造分页；表格按行线性化为带行号文本，公式保留原文文本形式。
- embedding 密钥通过本地环境配置读取，不写入资料包。查询与资料使用相同模型、维度和预处理版本；改变配置必须重建向量。
- 默认以 1024 维开展检索验证，记录实际返回维度；硅基流动接口提供维度参数，具体模型兼容性在接入阶段验证。([api-docs.siliconflow.cn](https://api-docs.siliconflow.cn/docs/api/embeddings-post?utm_source=openai))
- 管理端只导入资料包：经网页流式上传落盘，设单包大小上限，超限分包；提供校验、预览、元数据修正、发布、停用/启用及历史版本查看；修改影响分块或向量的内容必须重新生成资料包。
- 导入先进入草稿，校验完成后事务性发布；新版就绪才切换现行版本。替代关系由管理员确认，上传日期不代表生效日期，旧版不覆盖。
- 停用分两类：新版发布并确认替代关系时，旧版自动进入替代停用；手动停用由管理员主动执行，优先于替代停用——已被手动停用的资料即使后来被替代，仍保持手动停用。
- 两类停用共同点：历史引用仍可打开，原文及对应版本原文件仍可读；默认检索排除；用户端资料来源选择器隐藏，资料目录仅列现行资料。区别：明确询问往年时可检索替代停用的旧版，手动停用的资料任何检索不可达。重新启用已被替代的旧版须先解除替代关系，避免两个现行版本并存。
- 拒绝路径穿越、符号链接、异常解压规模、哈希及向量不匹配、重复导入（包哈希一致）的资料包；原文按安全文本展示，所有原文件接口都要求鉴权。
- 提供流式备份下载和恢复校验，包含资料、版本及管理设置，不包含聊天或 API 密钥；备份基于 SQLite 快照进行，期间不暂停读写；恢复期间暂停写入，并保留失败回滚路径。

## 四、技术实现与资源控制
- 前端采用 React＋TypeScript，管理端和用户端同一工程；后端采用 Python＋FastAPI＋LangGraph 核心库，SQLite 保存业务数据和全文索引。
- 中文文本经 jieba 分词后以空格分隔写入 SQLite FTS5，查询侧使用同一分词器；结合本地向量相似度检索，以排名融合选取证据。FTS5 提供全文检索基础能力。([sqlite.org](https://www.sqlite.org/fts5.html?utm_source=openai))
- 向量使用只读、分块扫描的 NumPy 文件；按当前有效或往年模式（纳入替代停用旧版）过滤版本、适用范围和明确文件限制后检索，不一次性将全部原文或向量载入内存。
- Agent 仅开放政策检索、原文上下文读取、版本查询三个只读工具，最多 3 轮工具调用；资料中的指令视为不可信内容。
- Gemini 接入保留原生工具调用所需的响应字段；验证反代兼容性。若失败，明确报错，不静默退化为无依据聊天。官方协议提供函数调用机制。([ai.google.dev](https://ai.google.dev/gemini-api/docs/function-calling?authuser=108&utm_source=openai))
- 引用由工具生成确定性 ID（文档哈希加行号区间），后端校验其是否确实来自本轮允许的证据；历史引用靠对资料库重解析打开，不依赖聊天历史，历史截断不影响旧引用；原文直接从资料读取，不让模型生成文件路径、页码或引文文本。
- API 覆盖鉴权、资料目录、原文读取、聊天事件流、取消请求、资料包导入与发布、备份恢复；公开类型包含版本、来源区间、范围选择和回答引用。
- 聊天历史由浏览器随请求提交，保留最近若干轮并设总字符上限（参数压测时定），自最旧截断；排队、检索、生成、引用、结束和错误通过事件流反馈，页面关闭或取消后释放任务资源。
- 断线即任务终止并释放服务端资源，仅在网络正常且浏览器处于前台时持续输出；断线时已显示的部分回答保留在本地记录并标注“已中断”，可据此重新提问。
- 初始同时处理 1 个问答，最多排队 10 个；同一浏览器只允许 1 个未完成请求。设置请求超时、限流及有界重试，压测后才提高并发。
- 前端在本地构建，服务器使用单后端进程和 systemd，接入现有 HTTPS 代理；不在服务器运行开发服务器或构建任务。
- 磁盘达到 8GB 提醒，导入前核算临时文件、索引和回滚所需空间；不足时拒绝导入，日志滚动限额，备份不在服务器积累。

## 五、实施顺序与验收
1. **兼容性验证：**使用少量真实资料验证 Gemini 工具调用、embedding 维度、中文检索、行号及版本引用，先确认完整链路。
2. **资料管理：**完成 Skill、资料包校验、导入预览、版本发布、原文阅读和备份恢复。
3. **用户问答：**完成本地历史、访问码、领域路由、跨领域 Agent、引用、排队及取消。
4. **安全与性能：**在 1C1G 限制下测试实际资料规模、10 个同时到达请求及大包导入，记录内存、磁盘、检索延迟、排队时间和 API 消耗。
5. **功能验收：**覆盖单领域、住宿与校纪及共青团组合、文件限制、不同学院年级、往年政策（替代停用旧版可检索）、冲突、无依据、刷新恢复和旧引用可访问；手动停用资料任何提问不可达但其历史引用可打开；断线后部分回答保留并标注已中断；更换访问码后旧码立即失效且已登录用户不受影响；管理员隐藏登录页登录及重置密码。
6. **异常验收：**覆盖恶意资料包、提示注入、越权访问、错误引用、API 限流、网络中断、磁盘不足、导入失败和备份恢复。
7. **上线门槛：**无 OOM、无未发布资料泄露、无服务端问答落盘；引用定位测试全部通过；功能与异常验收清单逐项通过——清单由实施方逐阶段提供，管理员逐项手动测试确认，容量类数据由脚本记录，清单未全部通过前明确阻止上线，不以“能启动”视为完成。

实施前仅需补齐：现有 HTTPS 接入方式、5～10 份可用于测试的资料。主模型与 embedding 的地址、模型名及密钥已保存在根目录 `deploy-config.md`（已加入 `.gitignore`），实施时经本地环境配置读取，不提交 git，不写入资料包。

## 六、审查修复与回归

本轮逐条处理代码审查的 18 项发现，保留前述产品行为与上线门槛。每个可运行模块独立本地提交，不推送；人工验收和真实 1C1G 压测仍由管理员执行，自动化结果不代替签署。

- [x] 测试基础：根目录命令可运行，数据目录按用例隔离，恢复测试验证备份后实际变更；合成资料代替未入库的私有测试包。
- [x] 1/11 安全：密钥使用请求头并过滤 HTTP 诊断日志，错误不回显上游正文，登录地址由显式可信代理配置解析；假密钥与伪造转发头回归通过。
- [x] 2/3/4/12 备份恢复：资料包纳入带哈希清单的 v2 备份、关联完整性验证、目录切换故障回滚、刷新签名器、恢复期间阻止请求并终止旧问答；v1 缺包备份需从原实例重新导出。
- [x] 5/6/9 导入发布：文件式有界校验、空间预检、后台串行处理、请求体上限、发布失败可重试及向量身份/范数校验；故障注入与无整包读取回归通过，真实 1C1G 内存门槛仍待部署压测。
- [ ] 7/8/10/13 检索与版本：执行时重算状态、往年模式、适用对象与章节过滤、启用一致性、可串联的工具标识。
- [ ] 14/15/16/17 前端：鉴权下载、显式范围扩展、增量持久化与中断恢复、管理员版本完整流程。
- [ ] 18 压测：预先获取会话、实时 SSE 计时、持续资源采样与用量记录。
- [ ] 总体验证：全量后端与前端测试、构建、离线浏览器回归；更新人工验收步骤及已知限制。

实现细化：资料包校验改用落盘文件和只读 mmap，限制 manifest/单文档大小以界定内存上限；恢复采用同文件系统暂存与保留回滚目录，校验数据库和所有引用文件后再切换。适用范围以可确认的学院、入学年份结构化条件过滤；未知或缺失条件明确提示澄清，不猜测自由文本标签。
